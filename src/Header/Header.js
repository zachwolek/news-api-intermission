import { Form } from '../Form/Form'
import './Header.css'

export const Header = () => {
    return (
        <header className='header-container'>
            <h1>News Every Minute NOW!</h1>
        </header>
    )
}