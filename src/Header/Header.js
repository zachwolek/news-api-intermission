import { Form } from '../Form/Form'
import './Header.css'

export const Header = ({handleAuthor}) => {
    return (
        <header className='header-container'>
            <h1>News NOW!</h1>
        </header>
    )
}